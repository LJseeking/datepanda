# =============================================================
# DatePanda — 环境变量模板
# 复制此文件为 .env 并填写真实值
# =============================================================

# ── 数据库（Neon PostgreSQL）────────────────────────────────
# 运行时连接（pgbouncer pooler，Next.js PrismaClient 使用）
DATABASE_URL="postgresql://user:pass@host-pooler.neon.tech/neondb?sslmode=require"

# 直连（prisma migrate deploy 使用，不经过 pgbouncer）
DATABASE_URL_UNPOOLED="postgresql://user:pass@host.neon.tech/neondb?sslmode=require"

# ── 应用 ────────────────────────────────────────────────────
APP_BASE_URL="http://localhost:3000"
# 生产：APP_BASE_URL="https://your-app.vercel.app"

# ── 邮件 ────────────────────────────────────────────────────
# console = 本地打印，不发送真实邮件
# smtp    = 使用 SMTP 发送真实邮件
EMAIL_PROVIDER="console"

# SMTP 配置（EMAIL_PROVIDER=smtp 时必填）
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your@email.com
# SMTP_PASS=your-smtp-password
# SMTP_FROM="DatePanda <noreply@datepanda.com>"

# ── Cron 鉴权 ────────────────────────────────────────────────
# Header: Authorization: Bearer <CRON_SECRET>
# 生成：openssl rand -hex 32
CRON_SECRET=your-cron-secret-here

# ── Debug 鉴权 ───────────────────────────────────────────────
# Header: x-admin-token: <MATCH_ADMIN_TOKEN>
# 本地可用 dev-admin-token，生产请设置强密码
MATCH_ADMIN_TOKEN=dev-admin-token

# ── Resend ───────────────────────────────────────────────────
RESEND_API_KEY=your_resend_api_key_here
MAIL_FROM="DatePanda <info@datepanda.fun>"
DEEPSEEK_API_KEY=
